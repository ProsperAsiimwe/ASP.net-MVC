@model SteppingStone.WebUI.Models.Account.ResetPasswordViewModel
@{
    ViewBag.Title = Model.Verb + " your password";
    Layout = "~/Views/Shared/_Console.cshtml";
    string companyName = Settings.COMPANY_NAME;
}

@*<div class="page-header">
    <div class="container">
        <h1>@ViewBag.Title</h1>
        <p>Enter your email address and a new password into the form below and click @Model.Verb Password to apply changes</p>
    </div>
</div>*@

<div class="login-page">
    <div class="login-form">
        @*<div class="text-center">
            <img src="~/Content/Imgs/ss_logo.png" class="login-logo" alt="@companyName" />
        </div>*@
        <div class="login-text">Enter your email address and a new password into the form below and click @Model.Verb Password to apply changes</div>
        <div class="form-dark-bg">
            @using (Html.BeginForm()) {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <div class="form-group">
                    @Html.Bootstrap().TextBoxFor(m => m.Email).Placeholder("Enter your email address").ShowValidationMessage(true)
                </div>
                <div class="form-group">
                    @Html.Bootstrap().PasswordFor(m => m.Password).Placeholder("Enter new password").ShowValidationMessage(true)
                </div>
                <div class="form-group">
                    @Html.Bootstrap().PasswordFor(m => m.ConfirmPassword).Placeholder("Confirm new password").ShowValidationMessage(true)
                </div>
                @Html.Bootstrap().SubmitButton().Style(ButtonStyle.Default).ButtonBlock().Text(Model.Verb + " Password").AppendIcon("fa fa-thumbs-up")
            }
            @*<div class="form-footer">
                @Html.Bootstrap().ActionLink("Back to login", "Index", "Home").AppendIcon("fa fa-sign-in")
            </div>*@
        </div>
    </div>
</div>

@*<section class="section-light">
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <section class="form-light-bg form-container">
                    @Html.Partial("Partials/_ResetPwd", Model)
                </section>
            </div>
        </div>
    </div>
</section>*@


@section Scripts {
    @*@Scripts.Render("~/bundles/jqueryval")*@
}
